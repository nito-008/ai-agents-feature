{
    "name": "OpenAI Codex",
    "id": "codex",
    "version": "1.2.0",
    "description": "Install OpenAI Codex CLI via npm.",
    "mounts": [
        {
            "source": "codex-credentials",
            "target": "/opt/codex-credentials",
            "type": "volume"
        }
    ],
    "postStartCommand": "ln -sfn /opt/codex-credentials ${HOME}/.codex",
    "options": {
        "codex_version": {
            "type": "string",
            "default": "latest",
            "description": "Version of @openai/codex to install with npm."
        },
        "node_version": {
            "type": "string",
            "default": "20",
            "description": "Major Node.js version to install if node/npm are missing."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}